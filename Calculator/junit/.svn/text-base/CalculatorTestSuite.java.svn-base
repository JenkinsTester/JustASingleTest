package test;

import static org.junit.Assert.*;

import org.junit.Test;

import org.junit.runner.RunWith;
import org.junit.runners.Suite;

/*
 * Zu einer Testsuite koennen mehrere Testklassen zusammengefasst werden.
 * 
 * Eine Test Suite ist eine Zusammenstellung von Testklassen, 
 * die zusammen ausgefuehrt werden sollen, denn oft moechte man 
 * Tests thematisch gruppieren, die eine gewissen Funktionalitaet, 
 * ein Modul oder einen Anwendungsteil testen. 
 */
@RunWith(Suite.class)
@Suite.SuiteClasses({ CalculatorTest.class, ParametricCalculatorTest.class })
public class CalculatorTestSuite {

}
